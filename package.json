{
  "name": "Pong.io",
  "version": "0.1.0",
  "description": "Not just Pong",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-client": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/server/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "backbone": "^1.3.3",
    "express": "^4.16.2",
    "rimraf": "^2.6.2"
  }
}
